server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/stock_db
    username: root
    password: root123
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848

seata:
  service:
    grouplist:
      default: seata-server:8091
