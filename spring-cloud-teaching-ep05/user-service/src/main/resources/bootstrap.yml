spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: TEACHING_GROUP
        refresh-enabled: true
      discovery:
        server-addr: localhost:8848
